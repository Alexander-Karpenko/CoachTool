spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/coach_tool
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

app:
  jwt:
    # Base64-encoded secret, min 256 bits (32 bytes). Change before production!
    secret: dGhpcy1pcy1hLXZlcnktc2VjcmV0LWtleS1mb3ItY29hY2h0b29sLWFwcA==
    expiration-ms: 86400000  # 24 hours